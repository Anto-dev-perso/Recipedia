appId: "com.recipedia"
---
- repeat:
    while:
      notVisible:
        text: ${recipeName}
    commands:
      - swipe:
          start: "80%,25%"
          end: "25%,25%"
          label: "Scroll random carousel until ${recipeName} is visible"

- tapOn:
    text: ${recipeName}
    label: "Open ${recipeName} recipe from random carousel"
