appId: "com.recipedia"
---
- tapOn:
    id: "RecipeTitle::OpenModal::RoundButton"
    label: "Tap on OCR button of recipe title"

- runFlow:
    file: ../android/addMedia.yaml
    env:
      FIELD_NAME: title
    when:
      platform: Android
# TODO iOS

- runFlow:
    file: ../android/selectMostRecentFromModal.yaml
    label: "Select title from modal for OCR"
    when:
      platform: Android
# TODO iOS
